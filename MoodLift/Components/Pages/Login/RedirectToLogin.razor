@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Always redirect unauthorized users to /login
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}
